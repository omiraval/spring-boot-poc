server:
  port: 8081

logging:
  level:
    org.springframework.security: debug

spring:
  application:
    name: oauth2-resource-server
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000